*vesper.nvim.txt*             For Neovim            Last change: 2025 March 26

==============================================================================
Table of Contents                              *vesper.nvim-table-of-contents*

1. Vesper                                                 |vesper.nvim-vesper|
  - Features                                     |vesper.nvim-vesper-features|
  - Requirements                             |vesper.nvim-vesper-requirements|
  - Installation                             |vesper.nvim-vesper-installation|
  - Usage                                           |vesper.nvim-vesper-usage|
  - Configuration                           |vesper.nvim-vesper-configuration|
  - Contributing                             |vesper.nvim-vesper-contributing|
  - License                                       |vesper.nvim-vesper-license|
2. Links                                                   |vesper.nvim-links|

==============================================================================
1. Vesper                                                 *vesper.nvim-vesper*

Port of the VS Code theme, Vesper <https://github.com/raunofreiberg/vesper> for
Neovim - written in Lua. The theme is designed to be easy on the eyes and
focuses on the code.


FEATURES                                         *vesper.nvim-vesper-features*

- Supports the latest Neovim <https://github.com/neovim/neovim> 0.9.0 <https://github.com/neovim/neovim/releases/tag/v0.9.0> features.
- Enhances terminal colors.
- Introduces a darker background option for sidebar-like windows.
- Supports all major plugins.


REQUIREMENTS                                 *vesper.nvim-vesper-requirements*

- Neovim <https://github.com/neovim/neovim> >= 0.7.2


INSTALLATION                                 *vesper.nvim-vesper-installation*

Install the theme with your preferred package manager, I recommended
folke/lazy.nvim <https://github.com/folke/lazy.nvim>

>lua
    {
        "PunGrumpy/vesper.nvim",
        lazy = true,
        priority = 1000,
        opt = {}
    }
<


USAGE                                               *vesper.nvim-vesper-usage*


VIM SCRIPT ~

>vim
    colorscheme vesper
<


LUA ~

>lua
    vim.cmd[[colorscheme vesper]]
<


CONFIGURATION                               *vesper.nvim-vesper-configuration*

Vesper <https://github.com/PunGrumpy/vesper.nvim> uses the default options,
unless `setup` is called.

>lua
    require("vesper").setup({
      -- your configuration comes here
      -- or leave it empty to use the default settings
      transparent = true, -- Enable this to disable setting the background color
      terminal_colors = true, -- Configure the colors used when opening a `:terminal` in [Neovim](https://github.com/neovim/neovim)
      styles = {
        -- Style to be applied to different syntax groups
        -- Value is any valid attr-list value for `:help nvim_set_hl`
        comments = { italic = true },
        keywords = { italic = true },
        functions = {},
        variables = {},
        -- Background styles. Can be "dark", "transparent" or "normal"
        sidebars = "dark", -- style for sidebars, see below
        floats = "dark", -- style for floating windows
      },
      sidebars = { "qf", "help" }, -- Set a darker background on sidebar-like windows. For example: `["qf", "vista_kind", "terminal", "packer"]`
      day_brightness = 0.3, -- Adjusts the brightness of the colors of the **Day** style. Number between 0 and 1, from dull to vibrant colors
      hide_inactive_statusline = false, -- Enabling this option, will hide inactive statuslines and replace them with a thin border instead. Should work with the standard **StatusLine** and **LuaLine**.
      dim_inactive = false, -- dims inactive windows
      lualine_bold = false, -- When `true`, section headers in the lualine theme will be bold
    
      --- You can override specific color groups to use other groups or a hex color
      --- function will be called with a ColorScheme table
      ---@param colors ColorScheme
      on_colors = function(colors) end,
    
      --- You can override specific highlights to use other groups or a hex color
      --- function will be called with a Highlights and ColorScheme table
      ---@param highlights Highlights
      ---@param colors ColorScheme
      on_highlights = function(highlights, colors) end,
    })
<


CONTRIBUTING                                 *vesper.nvim-vesper-contributing*

Contributions are welcome, please open an issue if you encounter any bug or if
you find any improvements are needed for the code, also feel free to open a PR.


LICENSE                                           *vesper.nvim-vesper-license*

MIT License <LICENSE> for more information.

==============================================================================
2. Links                                                   *vesper.nvim-links*

1. *preview*: ./assets/preview.png

Generated by panvimdoc <https://github.com/kdheepak/panvimdoc>

vim:tw=78:ts=8:noet:ft=help:norl:
